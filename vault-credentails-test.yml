- name: Read secret from Vault OIDC
  hosts: localhost
  connection: local
  tasks:
    - name: Display the password safely
      ansible.builtin.debug:
        # Use the variable name you defined in the AAP Credential mapping
        msg: "The password is {{ my_vault_password }}"
